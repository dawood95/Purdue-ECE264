ê

EXAMPLE



            exit(EXIT_FAILURE);

       #include <stdio.h>

       qsort - sorts an array

       #include <stdlib.h>

DESCRIPTION





       tively less than, equal to, or greater than the second.  If two members



       main(int argc, char *argv[])





              pointers to char", but strcmp(3) arguments are "pointers

       }

QSORT(3)                   Linux Programmer's Manual                  QSORT(3)

                  int(*compar)(const void *, const void *));

       greater  than  zero  if  the first argument is considered to be respec‚Ä


       in its command-line arguments:





           exit(EXIT_SUCCESS);

SEE ALSO

       description  of  the project, and information about reporting bugs, can

                                  2009-09-15                          QSORT(3)

       comparison  function  pointed  to  by  compar, which is called with two





       {





       The  qsort()  function sorts an array with nmemb elements of size size.



ê



       #include <stdlib.h>

COLOPHON









       static int

           for (j = 1; j < argc; j++)

RETURN VALUE

       {

           if (argc < 2) {

           qsort(&argv[1], argc - 1, sizeof(argv[1]), cmpstringp);

       sort(1), alphasort(3), strcmp(3), versionsort(3)

CONFORMING TO

NOTES

           /* The actual arguments to this function are "pointers to

           return strcmp(* (char * const *) p1, * (char * const *) p2);

       }



       The comparison function must return an integer less than, equal to,  or

       compare as equal, their order in the sorted array is undefined.

       tion can call strcmp(3), as shown in the example below.

       Another example is the following program, which sorts the strings given



       This page is part of release 3.35 of the Linux  man-pages  project.   A

NAME

SYNOPSIS

       void qsort(void *base, size_t nmemb, size_t size,



       The contents of the array are sorted in ascending order according to  a

       #include <string.h>

              to char", hence the following cast plus dereference */



ê

       sort(3) and versionsort(3).  To compare C strings, the comparison func‚Ä
       For one example of use, see the example under bsearch(3).

       cmpstringp(const void *p1, const void *p2)

       int

       be found at http://man7.org/linux/man-pages/.

       arguments that point to the objects being compared.

       The qsort() function returns no value.

           int j;

               puts(argv[j]);

       The base argument points to the start of the array.

       SVr4, 4.3BSD, C89, C99.

       Library routines suitable for use as the compar argument include alpha‚Ä
            fprintf(stderr, "Usage: %s <string>...\n", argv[0]);

           }





